<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="it.sanxia.dao.ILiuYanDao">
    <select id="findAll" parameterType="int" resultType="Liuyan">
        select * from tb_liuyan where user_id = #{u_id};
    </select>
</mapper>   